# determine directory?  or require it to be supplied?
# Substitute name of directory as required in various config files 
# and scripts.

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo "Adjust name and path to ${DIR}"
